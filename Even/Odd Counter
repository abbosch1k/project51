#include <iostream>
using namespace std;

int main() {
    int x, even=0, odd=0;

    for(int i=0;i<5;i++) {
        cin >> x;
        if(x%2==0) even++;
        else odd++;
    }

    cout << even << " " << odd;
}
